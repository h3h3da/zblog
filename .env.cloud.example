# 云端一键部署用（复制为 .env 后执行 docker-compose -f docker-compose.cloud.yml up -d）
# 仅部署：MySQL + 博客前端 + blog-api。管理后台在本地运行，通过 SSH 隧道连云端 MySQL。

# 数据库（blog-api 与本地管理后台共用）
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_USER=zblog
MYSQL_PASSWORD=your_secure_password
MYSQL_DATABASE=zblog

# 博客公网地址（用于 CORS、前端请求 API）
# 若前后端同域（Nginx 反向代理），可填 https://你的博客域名
BLOG_FRONTEND_URL=https://your-blog-domain.com
BLOG_API_URL=https://your-blog-domain.com/api
CORS_ORIGINS=https://your-blog-domain.com

# 管理员账号（blog-api 启动时在云端库中创建，供本地管理后台登录）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_admin_password
